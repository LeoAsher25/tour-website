# Taste
- Drives implementation tasks through a project prompt/spec file (e.g. `@init-prompt.md`, `@prompt/improve-ui-landing-after-init.md`) rather than describing requirements in chat; expects the agent to read and execute the referenced file, and uses `@`-mentions to point at the relevant files/sites. Work is organized as sequential named passes (init, then improvement/integration passes) scoped by these files. Confidence: 0.98
- When rebuilding a site, provides a reference URL/site clone and expects a faithful, content-accurate rebuild using the provided reference assets (images, real pricing, copy) rather than invented placeholder content. Confidence: 0.7
- Communicates tersely (e.g. just "continue", "Continue todo in checklist") and expects the agent to work autonomously through long, multi-step builds without asking for clarification or stopping for check-ins; on resume, expects the agent to pick up from the tracked checklist/todo state rather than restarting. (Seen again: two bare "continue" nudges during the integrate-api-and-booking implementation.) Confidence: 0.9
- When a URL/hash/signature value is shown in a message, expects the complete, unabridged value — questions ellipsized displays (e.g. noticing `vnp_SecureHash=...` and asking "có chính xác không? sao lại '...'"), because they inspect and verify exact values (like the 128-char hex hash) themselves rather than trusting a placeholder. Confidence: 0.8
- Is a Vietnamese speaker — writes some messages in Vietnamese even when the rest of the conversation is English (including entire requests in Vietnamese, e.g. a terse directive "Bỏ hết ... đi và chỉ dùng ... thôi"); responds well to updates/summaries delivered in Vietnamese. Confidence: 0.9
- Prefers a single canonical configuration source over multiple alternative paths: explicitly instructed to delete the individual Firebase Admin credential env vars (FIREBASE_PROJECT_ID / FIREBASE_CLIENT_EMAIL / FIREBASE_PRIVATE_KEY) from code, env files, and docs, keeping only FIREBASE_SERVICE_ACCOUNT_PATH (service account JSON file) — expects the "alternative/override" code branches and their doc references to be removed entirely, not left as fallbacks. Confidence: 0.95
- When the agent appears to stall or produces no visible output, re-sends the same `@file implement` command verbatim as a nudge rather than adding context, asking questions, or restating requirements (seen again: "implement @prompts/integrate-api-and-booking.md" re-sent verbatim right after the agent paused in plan mode, and the implement command repeated with slightly different path casing "@prompt/..."). Confidence: 0.85
- When the user reports something that looks like tool errors ("Failed to create"), they want a clear explanation that it was benign plus confirmation that work continued successfully — not a lengthy apology. Confidence: 0.6
- Approves agent-proposed refactoring plans by pasting the plan back and saying "continue to implement"; endorses centralizing repeated site constants (brand names, phone/WhatsApp, email, hours, social links, domain) into a single config module (e.g. `src/config/site.ts`) with helper functions like `getWhatsAppLink()` instead of hardcoding them across components, and expects exhaustive replacement even in content data (tour/blog/FAQ/itinerary copy) to be carried through to completion. Confidence: 0.8
- When asking about implementation details, points to exact file with `@`-path shorthand (e.g. `@components/motion/reveal.tsx`) and expects pinpoint answer with file path, line number, and code snippet (e.g. `viewport={{ once, amount: 0.2 }}` for scroll trigger = IntersectionObserver threshold, `offset: ["start end", "end start"]` for parallax) plus explanation of underlying API in Vietnamese when asked in Vietnamese. Confidence: 0.65
- When tuning motion/reveal, asks for semantic deep-dive on `ViewportOptions` (`amount`/`margin`/`root`/`once`) and custom props (`y`, `speed`, `offset`) as `IntersectionObserver`/`useScroll` wrappers — specifically that `amount` is element-visibility threshold (`some`=0, `all`=1, `0.5`=50% of the element itself not viewport) and actively calibrates trigger and timing params himself (e.g., `amount` `0.2` → `0.5` for later trigger, `duration` → `1` with `ease: "linear"`) and diagnoses per-property transition behavior (notices `opacity` interpolates 1s while `clipPath` remains abrupt/snap, requiring explicit per-prop `type: "tween"`) rather than accepting defaults or delegating fixes. Confidence: 0.85
- Reports animation regressions with precise, file-anchored visual QA — describing initial vs final states with quantitative clip-path values (e.g., `6%`/`4%` clipped small → sudden full) and noting missing interpolation — and expects diagnosis of `transition.duration`/`ease` and `viewport.amount` trigger timing before applying a fix. Confidence: 0.75
- When requesting a code fix, prefers receiving the complete, copy-paste-ready component file rather than partial snippets or diffs — explicitly asking in Vietnamese “cho tôi toàn bộ component MaskReveal đi” to get the full implementation with corrected per-property `clipPath`/`opacity` transitions. Confidence: 0.75

- Iterates hands-on during debugging: applies suggested tweaks to the code himself (e.g. changes `duration` to `1`, `ease` to `"linear"`, rewrites `MaskReveal`), then pastes the modified component back and asks the agent to diagnose why it still misbehaves — prefers explanation/diagnosis over the agent simply applying the fix. Confidence: 0.65
- Prefers motion/animation wrapper components to expose underlying Framer Motion props as optional passthrough (e.g., `viewport`, `transition` for Reveal/Stagger/MaskReveal/ZoomReveal and `offset` for Parallax) with defaults matching current hardcoded values, keeping original logic intact and allowing per-instance tuning without editing internals. Confidence: 0.9
- Expects scroll-reveal entrance animations to be clearly perceptible (starting from `opacity: 0` with `y: 16-18`) — considers subtle near-opaque starts (`opacity: 0.88-0.94`, `y: 12-14`) as effectively "no animation" and reports them as broken via terse file-anchored bug report (`@components/sections/intro-section.tsx`). Confidence: 0.75
- Expects hero/above-the-fold sections to occupy full viewport height (`min-h-[100svh]`) — reports shorter heights (`94svh`/`88svh`) as broken, especially when header is `fixed` overlay, via terse Vietnamese regression report (e.g., "Hero hiện tại không full height"). Confidence: 0.65
- Prefers solving header legibility over variable-brightness hero imagery with a subtle dark top scrim/gradient (e.g. `h-36`–`h-40` `rgba(8,12,10,0.42)` → `0.16` → `transparent`) to keep header transparent/cinematic, using high-opacity white text (`text-white/90`–`95`, `white/95` for brand location) with light `text-shadow` as secondary aid, and explicitly rejects full-width opaque/glass/white header backgrounds at the top that break the cinematic feel. Confidence: 0.85
- When reporting a UI/visual bug, delivers a ranked solution hierarchy with copy-paste-ready Tailwind/CSS snippets (exact gradient, `rgba`, `text-white/*` classes, `text-shadow` values) and file targets (`site-header.tsx` + Hero scrim) for direct application rather than open-ended discussion. Confidence: 0.8
- Expects client-side route transitions to reset scroll instantly to top without smooth tweening — reports it as a bug when navigating (e.g., landing → View tour → detail page) smoothly scrolls from the click position and sequentially triggers all destination `whileInView`/IntersectionObserver entrance animations, leaving nothing to reveal on arrival. Confidence: 0.75
- Expects payment/booking integrations to be security-aware and production-grade: prices computed server-side (never trusting client-submitted amounts), payment signatures verified, IPN/webhook handlers idempotent, bookings addressed by generated codes, and automated tests covering tampered prices, double bookings, and sold-out states. The "no silent mock fallback" rule applies to real production data errors — when an external dependency is configured but unprovisioned/unreachable (e.g. Firestore throwing `5 NOT_FOUND` because the DB was never created), the site must degrade gracefully to local/seed data with loud logging rather than crash site-wide, and switch to the live database automatically once it's provisioned. Confidence: 0.65
- Reports runtime/server errors by pasting the raw stack trace / error log verbatim (e.g. `⨯ Error: 5 NOT_FOUND:` or `[bookings] create failed Error: ...` with file:line references) as a terse directive (e.g. "when booking, get error" followed by the log) and expects autonomous root-cause diagnosis, a fix, and verification (build/tests/HTTP checks) without clarification questions or lengthy apologies. Seen again in a fresh report pasting two distinct failures at once (a 500 booking error from Firestore `undefined` field values and a home-page 500 from a missing composite index) plus matching browser console errors — expects each error in the paste to be triaged and fixed; and again as a terse Vietnamese follow-up ("và vẫn lỗi \"Sai chữ ký\"") expecting continued autonomous diagnosis of the new URL. Confidence: 0.9
- For API bugs, provides the exact reproduction command — a full `curl` copied from the browser devtools including request headers, session cookie, and JSON body — and expects it to be run verbatim to reproduce and then re-run to verify the fix. (Seen again: a fresh VNPay bug report with a complete curl incl. session/CSRF cookies and `{"bookingCode":"JAS-2J52VN"}` body, plus the exact response JSON and the VNPay error URL/code for reproduction; and again when reporting the portal "Test call IPN" result — pasting the full XHR curl with all `__RequestVerificationToken`/session cookies and the raw `{"RspCode":"97","Message":"Invalid signature"}` response verbatim rather than summarizing.) Confidence: 0.9
- Expects the agent to self-test complete user flows end-to-end after implementing or fixing ("Sau đó, tự mình test flow booking để đảm bảo thông luồng" — then test the booking flow yourself to make sure it works through): running real HTTP requests against the running dev server to walk the full journey (create booking → create payment URL → send a correctly signed IPN callback → confirm status transitions → repeat for idempotency → tampered signature rejection → alternate payment method → pages render), using the actual env credentials, rather than relying only on unit tests/typecheck. Confidence: 0.8

- When a persistent signature/hash error is being diagnosed in an external integration, proactively captures and sends a screenshot of the external provider's admin portal configuration (e.g., VNPay sandbox "Kiểu mã hóa" = HMACSHA512, IPN URL fields) as evidence with a terse hypothesis ("Hình như kiểu mã hoá khác") — expecting the agent to cross-check the code against the portal settings rather than assume the algorithm is wrong. Confidence: 0.6
- When a persistent signature/hash error is being diagnosed in an external integration, proactively captures and sends a screenshot of the external provider's admin portal configuration (e.g., VNPay sandbox "Kiểu mã hóa" = HMACSHA512, IPN URL fields) as evidence with a terse hypothesis ("Hình như kiểu mã hoá khác") — expecting the agent to cross-check the code against the portal settings rather than assume the algorithm is wrong. Confidence: 0.6
- For payment integration debugging, expects forensic verification of signatures and data against the live config: recomputing HMAC hashes from the exact current env secret across multiple encoding variants (URLSearchParams/`+`, raw, RFC3986/`%20`) to prove which value was actually signed, correlating timestamps (URL `vnp_CreateDate` vs env file mtime vs server start) to detect stale secrets, comparing JS encoding against the gateway's PHP semantics, and verifying booking/amount data directly in the real database to rule out the payment code itself as the root cause before concluding (as with a VNPay "Sai chữ ký"/code 70 report). Confidence: 0.8
- Follows up by re-creating a fresh payment URL from a real DB booking and verifying its hash matches the current secret to confirm the code path is correct — accepting the diagnosis that an old URL/secret or mismatched environment data caused the error rather than the code. Confidence: 0.7
- During multi-stage integration debugging, expects the agent to re-test the same parameter across a sweep of values against the live sandbox (e.g., `vnp_CreateDate` at −2/−5/−10/−15/−20/−30 min, `vnp_ExpireDate` at +15/+30/+60) and to re-interpret earlier "accepted" results when a new error stage invalidates them — including correcting the agent's own prior conclusions (e.g., realizing code 15 from curl did not mean the payment page would load in a real browser, and that a fresh booking/txnRef also fails) rather than holding to an earlier hypothesis. Confidence: 0.7
- When all sandbox requests fail with the same generic error regardless of parameters, expects the conclusion to shift from code/time issues to provider-side configuration (e.g., account state, IPN URL set to `#`/invalid on the portal) — with a clear handoff of the portal actions only the user can take (entering a valid HTTPS IPN URL via tunnel for local testing) and an offer to set up the tunnel, rather than endless further code changes. Confidence: 0.6
- When diagnosing an integration failure, checks infra-level causes alongside code (e.g. that a payment gateway's server cannot reach a `http://localhost` IPN URL, leaving transactions stuck `processing`) and flags them as a separate, more serious problem to fix (tunnel/public URL for sandbox testing), while still answering the immediate question. Confidence: 0.75
- When a root cause is explained in terms of dev-environment behavior, immediately probes the production implication ("thế sau lên prod thì sao?") — expects the answer to explicitly separate dev-only behavior (e.g. `.env` loaded once at server start, requires restart, no hot reload) from production (env injected at deploy time, always fresh) and give a definitive verdict on whether the issue can recur in prod. Seen again when the root cause was a sandbox-only condition (VNPay sandbox clock running ~15 min slow): the user asked what "production" means in this context, whether waiting for the sandbox provider to fix it is viable, and what the merchant admin portal is — before deciding on any workaround — expecting a clear separation of sandbox/test behavior from production and a recommendation that doesn't risk production. Confidence: 0.75
- Reports the next error state in an external integration by attaching a screenshot of the actual provider error screen (e.g., VNPay sandbox page showing "Giao dịch đã quá thời gian chờ thanh toán" / code 15) with only a terse label ("Lỗi mới") — expecting the agent to read the screenshot, recognize which failure stage it represents (e.g., signature now valid, new timeout/config layer), and continue autonomous root-cause diagnosis, hypothesis testing, and fresh-experiment verification without asking for clarification. Confidence: 0.85
- During multi-step external-integration debugging, executes the "only the user can do" handoff steps himself (set up the ngrok tunnel for local sandbox testing; filled the VNPay portal IPN URL with the HTTPS ngrok domain, GET protocol, HMACSHA512) and then reports back tersely with a status + "Nên làm gì tiếp" ("what next?") — expecting the agent to verify the setup end-to-end, keep driving with a concrete ordered next step, and handle all code/env changes (e.g. updating Return/IPN URLs in `.env` to the tunnel domain) without asking him to do anything else. (Seen again: when the agent again handed off only-portal steps, the user pushed back with "Bạn có thể tự call api để test không?" — preferring the agent to exhaust its own API-level verification before asking anything of him.) Confidence: 0.85
- During external-integration debugging, proactively uses the external provider's own native tooling/portal features (e.g., VNPay sandbox's "Tạo link thanh toán") to generate a control URL/hash that bypasses his own code entirely — presenting the provider-generated artifact as evidence to conclusively isolate whether a persistent failure (e.g., code 70 "Sai chữ ký") is code-side or provider-side, and expecting the agent to verify the artifact against the current secret before declaring the fault provider-side. Confidence: 0.7
- When a persistent integration issue resists diagnosis, directs the agent to re-read the provider's official documentation ("Thử đọc lại docs xem có issue gì không?" + the docs URL) to hunt for missed parameter/encoding/timezone requirements before concluding provider-side fault — while the user concurrently pursues a parallel isolation path (creating a fresh provider test account) to test whether the problem is specific to the current account. Confidence: 0.75
